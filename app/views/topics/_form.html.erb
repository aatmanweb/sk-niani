<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<center>
<table width="100%" border="0" cellspacing="0" cellpadding="0" style="font-family:Kalinga, Arial, Helvetica, sans-serif; font-size:16px;">

    <tr>
    <td width="32%" align="right"><%= f.label(:topic_name) %></td>
    <td width="2%">&nbsp;</td>
    <td width="66%" align="left"><%= f.text_field(:topic_name, :class => 'innerpages-text-input') %></td>
    </tr>
</table>
  <div class="form-buttons">
    <%= f.submit({:class => 'innerpage-form-button'}) %>
  </div>
</center>
<% end %>
